# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Algorithmic Alignment Group
email: thestephencasper@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Researching frameworks for human-aligned AI @ MIT CSAIL.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://algorithmicalignment.csail.mit.edu" # canonical site; used for absolute URLs in feed.xml, canonical tags, etc.
twitter_username: slcasper
github_username: thestephencasper

# Build settings
theme: jekyll-theme-slate
plugins:
  - jekyll-feed

load_paths:
  - _sass
  - node_modules

sass:
    sass_dir: ./_scss

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# Keep internal / repo-only files out of the built site.
# Files without YAML front matter are otherwise copied verbatim into _site
# and served publicly.
exclude:
  - deploy.sh
  - claude.md
  - CLAUDE.md
  - README
  - README.md
  - Gemfile
  - Gemfile.lock
  - .idea/
  - venv/
  - vendor/
